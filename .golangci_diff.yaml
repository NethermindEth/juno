version: "2"
linters:
  default: none
  enable:
    - lll
    - nolintlint
    - staticcheck
  settings:
    lll:
      line-length: 100
    nolintlint:
      allow-unused: false
      require-explanation: true
    staticcheck:
      checks:
        - "SA1019"
  exclusions:
    rules:
      - linters:
          - lll
        source: '^//go:generate |https?://'
run:
  timeout: 10m
