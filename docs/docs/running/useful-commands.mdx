---
title: Useful Commands 
---
# 🚚 Run Locally

Install dependencies:

```bash
make install-deps
```

In the `juno` directory, run the following to compile Juno:

```bash
make juno
```

To run tests:

```bash
make test
```

For more details of the makefile, just run:

```bash
make help
```

For a full list of options, use:

```bash
./build/juno -h
```
